datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "./repro-client"
}

model DatasetSnapshot {
  id     String @id
  status String
}
